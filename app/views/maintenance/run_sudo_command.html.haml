.text-center
  .lead= I18n.t('admin_index.execute_sudo_command')
  %i= I18n.t('admin_index.execute_sudo_command_info')

.row.span11

.row.span11
  = form_tag( :run_sudo_command, class: 'form-horizontal', :onsubmit => "$('#loadingIndicator').show(); ") do
    .control-group
      = label_tag( :sudo_cmd, t('admin_index.sudo_cmd'), :for => :sudo_cmd, class: 'control-label' )
      .controls
        = text_field_tag( :sudo_cmd, nil, class: 'input-xxlarge' )
    .control-group
      = label_tag( :password, t(:password), :for => :password, class: 'control-label' )
      .controls
        = password_field_tag( :password, nil, class: 'input-large' )

    .control-group
      .controls
        = submit_tag( I18n.t('admin_index.go'), :data => {:disable_with => I18n.t(:please_wait), class: 'btn disabled'}, class: 'btn' )
        %span(id='loadingIndicator' hidden='true')
          = image_tag( 'indicator.gif' )

.row.span11
  - if (@console_output && @console_output.size > 0)
    %hr
    %pre.prettyprint.linenums= @console_output
