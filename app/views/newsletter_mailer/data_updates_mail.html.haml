%h2= "--=[  Goggles  -o^o-  ]=-- #{ t('newsletter_mailer.data_updates.generic_title') }"

- if @meeting_array.respond_to?( :each )
  %h3= t('newsletter_mailer.data_updates.meeting_data_title')
  %p= t('newsletter_mailer.data_updates.meeting_data_has_been_updated')

  %ul
    - @meeting_array.each do |meeting|
      %li
        / Quick hack to avoid having a useless route helper defined inside Admin app:
        = link_to( meeting.description, "/meeting/show_full/#{ meeting.id }" )
        = " (#{ meeting.header_year })"
